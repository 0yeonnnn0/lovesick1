# Prompt Draft

현재 기본 프롬프트는 코드에 있는 preset 시스템을 기준으로 관리한다.

- 기본 응답: `src/bot/prompt/default-presets.ts`
- 질의형 응답: `src/bot/ai.ts`의 memory query prompt
- 기억 추출: `src/bot/ai.ts`의 extraction prompt

별도 초안 문서가 필요하면 여기서 이어서 정리한다.
